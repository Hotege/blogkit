Blog Kit

requires docker, upx
required image:
    golang:1.12.6
    alpine:3.9.4

article decode:
    l -> label(context)
    t -> text(content)
    i -> image(path)
    a -> attachment(path)
    c -> code(content)
